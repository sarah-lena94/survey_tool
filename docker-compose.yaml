version: "3.8"
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - db
  db:
    image: "h2database/h2database:latest"
    ports:
      - "8082:8082"
    environment:
      H2_OPTIONS: "-ifNotExists"